<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.albert.measure">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.HeightActivity"></activity>
        <activity android:name=".activities.AboutUsActivity" />
        <activity android:name=".activities.AddAreaActivity" />
        <activity android:name=".activities.AddVectorActivity" />
        <activity android:name=".activities.AddElementActivity" />
        <activity
            android:name=".activities.ResultsActivity"
            android:label="@string/title_activity_results"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.PointMethodActivity" />
        <activity android:name=".activities.AddPointActivity" />
        <activity android:name=".activities.AddAngleActivity" />
        <activity android:name=".activities.AddVolumeActivity" />
        <activity android:name=".activities.SensorTestActivity" />
    </application>

    <uses-permission android:name="android.permission.CAMERA" />

</manifest>